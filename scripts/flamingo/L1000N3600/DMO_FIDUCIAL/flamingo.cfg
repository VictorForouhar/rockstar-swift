# Simulation params - Other values are read from the snapshot file
FILE_FORMAT = "SWIFT"

# Force resolution in Mpc/h
FORCE_RES = 0.00759
FORCE_RES_PHYS_MAX = 0.0019408

# Data input
INBASE = "/cosma8/data/dp004/flamingo/Runs/L1000N3600/DMO_FIDUCIAL/snapshots/"
FILENAME = "flamingo_<snap>/flamingo_<snap>.<block>.hdf5
NUM_BLOCKS = 960

SNAPSHOT_NAMES = "../snapshot_names.txt"
OUTBASE = "/snap8/scratch/dp004/dc-foro1/halo_comparison/L1000N3600/DMO_FIDUCIAL/Rockstar_exclusive"

# Parallel Processing
PARALLEL_IO = 1
FORK_READERS_FROM_WRITERS = 1 # Writer tasks will start reader tasks
NUM_WRITERS = 1024 # Total number of CPUs across all nodes (128 * 8). NEEDS to be divisible by 8.
FORK_PROCESSORS_PER_MACHINE = 1 # Split each writer task that is launched into this many processes
PARALLEL_IO_SERVER_INTERFACE = "ibp33s0" # Correct value for internode communication after COSMA OS update.

# Output format
OUTPUT_FORMAT = "BINARY"

# To better compare to other halo finders.
INCLUSIVE_OR_EXCLUSIVE_MASS = "EXCLUSIVE"